# 2026-08-12

## Summary

- Added the SSE child project under `sse-t0/` for offline Shanghai feed and model work.
- Kept SSE and SZE factor contracts separate; SSE uses the supplied 50-factor v0.4 order.
- Added a native C++ SSE model runtime and an offline NPZ conversion tool. PyTorch remains reference-only.
- Added a reusable UDP channel runtime for future SSE/SZE receiver sharing.
- Documented FPGA and LV1 endpoints from the supplied server information file.

## Changelog

- `modules/deepwin_guoxin/md/common/UdpChannelRuntime.*`: exchange-neutral UDP bind, multicast join, receive metadata, and callback lifecycle.
- `sse-t0/model/sse_model_runtime.*`: fixed-layout `SSEMODL1` loader and residual-stack/GRU/LayerNorm/head inference.
- `sse-t0/model/convert_state_dict_npz.py`: offline conversion from the reference NPZ state dict.
- `sse-t0/strategy/sse_get_obj.cpp`: fail-closed until the native SSE factor pipeline is connected; prevents accidental use of the legacy SsePredictor path.
- `sse-t0/docs/LV1_ENDPOINTS.md`: SSE FPGA UDP and Zhongchang TCP L1 address inventory.

## Verification

- CentOS 7.9 builder fingerprint passed (`gcc/g++ 4.8.5`, `cmake 3.17.5`, `python 3.6.8`).
- Built SSE observer, shared market-data runtime, native model runtime, and config tests.
- Passed `sse_model_runtime_test`, `sse_config_guard_test`, `sse_udp_observer_test`, and offline loopback UDP capture.
- Native zero-input golden parity passed. The online row-by-row 4,102-row parity run reached `1.0728836e-6` prediction max absolute error and `1.1324883e-6` hidden max absolute error against the reference batch CPU oracle; the bundle threshold is `1e-6`, so full continuous parity remains open.
- Added the live-only package under `src/t0-main/build/configs/sse-live-capture/`; it contains the observer binary, Dongguan/Jinqiao manifests, start/stop scripts, dual-clock JSONL fields, relation analyzer, endpoint notes, and SHA256 manifests. Final archive: `sse-live-capture-20260813_live_capture_v3.tar.gz`.
- Package verification passed: archive extraction, inner file hashes, binary build, two-channel loopback capture, JSON/Python/Bash syntax checks, and CentOS 7.9 builder fingerprint. `ts_ns` uses Linux `SO_TIMESTAMPNS` when available, with a user-space fallback.
- Added the live-only package under `src/t0-main/build/configs/sse-live-capture/`; it contains the observer binary, Dongguan/Jinqiao manifests, start/stop scripts, dual-clock JSONL fields, relation analyzer, endpoint notes, and SHA256 manifests.
- Package verification passed: archive extraction, inner file hashes, binary build, two-channel loopback capture, JSON/Python/Bash syntax checks, and CentOS 7.9 builder fingerprint.
