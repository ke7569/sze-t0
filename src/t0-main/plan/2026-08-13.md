# 2026-08-13

## Summary

Aligned Shenzhen startup position and T0 capacity semantics with the proven Beijing/HP implementation. Added conservative retry/cutoff handling for incomplete startup position responses and removed the Shenzhen-only afternoon bias multiplier.

## Changelog

- Confirmed local `HEAD` and `origin/master` are both `b04a702`; preserved pre-existing local SSE/config-generator work.
- Added `sze_position_risk.h` with testable startup normalization, buy/sell capacity, bias, unit-bias, and cutoff helpers.
- Changed Shenzhen buy capacity to `min(shortable, position_limit) - pi - pending_buy`.
- Removed `cum_buy` from Shenzhen sellable inventory because Shenzhen A shares are T+1 and broker `Available` is authoritative.
- Startup position synchronization now keeps `last_position == pi == total_position - static_position`.
- Missing instruments remain routing-gated, retry every 5000 ms, and default to `0/0` only at the configurable 09:31:00 cutoff.
- Restored a fixed configured bias factor; removed the incorrect HHMM-based afternoon multiplier.
- Added config validation for retry interval and HHMMSS cutoff.
- Validated the environment with `check_centos79_build_env.sh`: CentOS 7.9.2009, GCC/G++ 4.8.5, glibc 2.17, Python 3.6.8.
- Built `libt0_strategy_sze.so` in `src/t0-main/build/sze-position-risk-parity`; position-risk build SHA256 `aeaf37c8efa6bafd234f77d88cfcf56815123d7559d84184c58b0f5ca5b58c15`.
- Passed `sze_position_risk_test` and `sze_config_guard_test`; ABI symbol and `ldd` checks passed.
- Existing Python config tests still have two unrelated Python 3.6 ASCII CSV failures on the literal Chinese unit string.

## Startup prediction warmup

- Added `sze_startup_warmup_signals` with default `50` to Shenzhen strategy configuration.
- The first 50 per-instrument dispatched samples now run `calcTheo` and remain prediction-only; `handleT0`, test orders, and regular order submission are suppressed.
- The 51st sample is the first sample eligible for normal trading, subject to position/routing readiness.
- The daily config generator now emits the warmup field automatically.
- Final warmup build SHA256: `1024005fe71ac54a640072cbc284086f92bf028f6995d1fff4effaaadcdbb432`.
