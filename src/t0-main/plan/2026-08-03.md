# 2026-08-03

- Generated the v04 Shenzhen all-market universe from 20260731 static data.
- Embedded FreeShare and five-day HistoryAmount for 2,892 instruments; rejected 0.
- Planned eight deterministic recovery shards with disjoint symbol ownership.
- Built a `/home/zane`-mirrored deployment package with generic config filenames.
- Updated capture/recovery launch validation for the v04 model and strategy build.
- Added artifact checksums and deployment instructions.
- Diagnosed live recovery processes with no journal/SHM descriptors. The consumer
  code was present in the SO; the pre-open CPU-affinity failure path could exit
  the worker silently from the process-health perspective.
- Changed affinity failure to log and continue unpinned, added synchronous
  recovery-start verification in the SZE factory, and bumped the build id to
  `sze-strategy-20260803-v04-recovery-start-v2`.
- Fixed the preflight build-id check so `set -o pipefail` cannot turn grep's early
  success into a false `unexpected_strategy_build_id` failure.
- Replaced the thread-entry handshake with an online attach handshake. The SZE
  factory now returns success only after both the journal and SHM are open and
  their trading-day/source/generation metadata match. Added structured open
  statuses and bumped the build id to
  `sze-strategy-20260803-v04-recovery-attached-v3`.
- Built `sze-dev-debug-20260803`, a Shenzhen-only development/debug bundle with
  the `/home/zane` deployment mirror, target-specific strategy and MD sources,
  v04 model, selected order/trade and UDP samples, HP/mdserver references,
  bundled Deepwin/Boost/Python headers and runtime libraries, and a recorded
  CentOS 7.9 environment fingerprint. Verified an independent portable rebuild
  of both strategy and MD libraries plus recovery/protocol/config tests.
